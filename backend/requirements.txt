# Runtime deps for the Vercel Python function (mirrors pyproject [project].dependencies,
# minus uvicorn which the serverless runtime provides). Dev/test deps are not installed here.
fastapi>=0.110
pydantic>=2.6
pydantic-settings>=2.2
pyjwt>=2.7
httpx>=0.27
cryptography>=42
